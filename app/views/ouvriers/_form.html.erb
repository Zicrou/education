<div class="col-sm-8 blog-main">

  <%= form_with(model: ouvrier, local: true) do |form| %>
    <% if ouvrier.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(ouvrier.errors.count, "error") %> prohibited this ouvrier from being saved:</h2>

        <ul>
        <% ouvrier.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="col-md-12">
      <div class="form-group">
        <%= form.label :name %>
        <%= form.text_field :name, class:"form-control"%>
      </div>

      <div class="form-group">
        <%= form.label :prenom %>
        <%= form.text_field :prenom, class:"form-control"%>
      </div>

      <div class="form-group">
        <%= form.label :telephone %>
        <%= form.text_field :telephone, class:"form-control"%>
      </div>

      <div class="actions">
        <%= form.submit class:"form-control btn-primary" %>
      </div>
    <% end %>
    </div>

</div>