<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @metier.name %>
</p>

<p>
  <strong>Name:</strong>
  <%#= @metier.user.name !if logged_in?(:site_admin, :trusted)%>
</p>

<%= link_to 'Edit', edit_metier_path(@metier) %> |
<%= link_to 'Back', metiers_path %>
