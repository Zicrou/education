<div class="col-sm-8 blog-main">
  <%= form_with(model: juri, local: true) do |form| %>
    <% if juri.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(juri.errors.count, "error") %> prohibited this juri from being saved:</h2>

        <ul>
        <% juri.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= form.label :numjuri %>
      <%= form.text_field :numjuri, class:"form-control" %>
    </div>

    <div class="form-group">
        <%= form.label :centre_id %>
        <%= form.collection_select(:centre_id, Centre.all, :id, :numcentre,
                                  {
                                    include_blank: true
                                  },
                                  class: 'form-control'
                                ) 
        %>
      </div>

    <div class="actions">
      <%= form.submit class:"form-control btn btn-primary"%>
    </div>
  <% end %>
</div>