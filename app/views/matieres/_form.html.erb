<br\><br\><br\>
<br>
<br>
<br><%= form_with(model: matiere, local: true) do |form| %>
  <% if matiere.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(matiere.errors.count, "error") %> prohibited this matiere from being saved:</h2>

      <ul>
      <% matiere.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :seri_id %>
    <%= form.collection_select(:seri_id, Seri.all, :id, :name,
                              {
                                #selected: blog.topic_id,
                                include_blank: true
                              },
                              class: 'form-control'
                            ) 
  %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
